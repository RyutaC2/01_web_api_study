# プロジェクト概要 / Project Overview

ユーザー登録・ログイン機能を持つWebアプリケーションの学習用プロジェクト。
フロントエンド → バックエンド → DBの3層構成。

---

## 技術スタック

| 役割 | 技術 |
|---|---|
| フロントエンド | Next.js (App Router) |
| バックエンド | Go言語 |
| データベース | PostgreSQL 16 |
| インフラ | Docker / Docker Compose |
| 認証 | JWT (JSON Web Token) |

---

## ディレクトリ構成

```
01_web_api_study/
├── front_end/           # Next.jsのコード一式
│   └── app/
│       ├── components/  # 再利用するUIパーツ
│       ├── user/
│       │   ├── login/page.tsx     # ログイン画面
│       │   ├── register/page.tsx  # 新規登録画面
│       │   └── settings/page.tsx  # ユーザー設定画面
│       └── about/       # 自己紹介ページ群
├── back_end/            # Goのコード一式
│   ├── main.go          # エントリーポイント（現在ほぼ空）
│   ├── go.mod           # Goの依存関係管理ファイル
│   └── go.sum           # 依存関係のチェックサム
└── deploy/              # インフラ設定
    ├── docker-compose.yaml   # 3サービスの構成定義
    ├── backend.Dockerfile    # Goサーバーのコンテナ定義
    ├── frontend.Dockerfile   # Next.jsのコンテナ定義
    ├── .env                  # 環境変数（Git管理外）
    └── .env.example          # 環境変数のサンプル
```

---

## システムの通信フロー

```
ブラウザ (ユーザーが操作)
    │
    │ HTTPリクエスト（例: POST /api/register）
    ▼
Next.js フロントサーバー（ポート3000）
    │  ※ブラウザとGoの中継役。Route Handlerで処理。
    │  ※JWTはここでCookieにセット/取り出しを行う。
    │
    │ HTTPリクエスト（例: POST http://backend:8080/api/users/register）
    ▼
Goバックエンドサーバー（ポート8080）
    │  ※ビジネスロジック（パスワードハッシュ化、JWT生成など）
    │
    │ SQLクエリ
    ▼
PostgreSQL（ポート5432）
    │  ※データの永続化
```

---

## 認証方式（JWT + httpOnly Cookie）

### なぜこの方式か
- JWTをブラウザの `localStorage` に保存すると、XSS攻撃でトークンが盗まれるリスクがある
- `httpOnly` なCookieに保存すると、JavaScriptから読めないため安全
- Next.jsのRoute Handler（サーバー側）がCookieをセットすることでhttpOnlyを実現

### ログイン時のフロー
```
1. ブラウザ → Next.js Route Handler: POST /api/login { identifier, password }
2. Next.js → Goバックエンド: POST /api/users/login { identifier, password }
3. Go: DBからユーザー取得 → bcryptでパスワード照合 → JWTトークン生成
4. Go → Next.js: { token: "eyJhbGci..." }
5. Next.js: Cookieに token をhttpOnlyでセット → ブラウザへレスポンス
6. 以降のリクエストでブラウザがCookieを自動送信
```

### 認証が必要なAPIのフロー
```
1. ブラウザ → Next.js Route Handler: GET /api/user/settings (Cookieが自動付与)
2. Next.js: CookieからJWTを取り出し、Authorizationヘッダーに付けてGoへ転送
3. Go: JWTを検証 → ユーザーIDを取り出す → DBからデータ取得
4. Go → Next.js → ブラウザ: データを返す
```

---

## 現在の実装状況

### 完了済み ✅
- [x] Docker Compose 構成（3サービス: frontend, backend, db）
- [x] frontend.Dockerfile / backend.Dockerfile
- [x] Goの基本依存関係（chi, pgx）
- [x] Next.jsのフォーム実装（登録・ログイン画面、バリデーション付き）
- [x] 1. HTTPサーバー起動 + chiルーター設定（main.go）
- [x] 2. PostgreSQL接続処理（pgxpool、DSN組み立て、Ping確認）

### 未実装 ⬜（実装順に記載）

#### Goバックエンド
- [ ] 3. usersテーブルのSQL定義（DBマイグレーション）← **golang-migrate を使用**
- [ ] 4. POST /api/users/register → bcryptハッシュ化してDB保存
- [ ] 5. POST /api/users/login → パスワード照合 → JWT発行
- [ ] 6. JWTミドルウェア（認証必須ルートに適用）
- [ ] 7. GET /api/users/me など認証済みAPIの実装

#### Next.jsフロントエンド
- [ ] 8. Route Handler: POST /api/register → Goの登録APIへ転送
- [ ] 9. Route Handler: POST /api/login → GoのログインAPI → CookieにJWTをセット
- [ ] 10. register/page.tsx の onSubmit でRoute Handlerを呼ぶ
- [ ] 11. login/page.tsx の onSubmit でRoute Handlerを呼ぶ
- [ ] 12. 認証状態の管理（ログイン中かどうかのUI切り替え）

---

## Goの依存関係

### 導入済み
| パッケージ | 用途 |
|---|---|
| `github.com/go-chi/chi/v5` | HTTPルーター |
| `github.com/jackc/pgx/v5` | PostgreSQLドライバ |

### 追加が必要
| パッケージ | 用途 | インストールコマンド |
|---|---|---|
| `github.com/golang-migrate/migrate/v4` | DBマイグレーション管理 | `go get github.com/golang-migrate/migrate/v4` |
| `github.com/golang-migrate/migrate/v4/database/postgres` | golang-migrate PostgreSQLドライバ | `go get github.com/golang-migrate/migrate/v4/database/postgres` |
| `github.com/golang-migrate/migrate/v4/source/file` | golang-migrate SQLファイル読み込み | `go get github.com/golang-migrate/migrate/v4/source/file` |
| `github.com/golang-jwt/jwt/v5` | JWT生成・検証 | `go get github.com/golang-jwt/jwt/v5` |
| `golang.org/x/crypto` | bcryptパスワードハッシュ | `go get golang.org/x/crypto` |

---

## データベース設計（予定）

### usersテーブル
| カラム名 | 型 | 説明 |
|---|---|---|
| id | UUID | 主キー |
| username | VARCHAR(12) | ユーザー名（3〜12文字、英数字・_・-のみ） |
| email | VARCHAR(255) | メールアドレス（一意） |
| password_hash | VARCHAR(255) | bcryptでハッシュ化されたパスワード |
| created_at | TIMESTAMPTZ | 作成日時 |

---

## 重要な注意事項（AI向け）

- **ユーザーは超初心者のため、コードの各部分を丁寧に説明すること**
- **Next.jsのRoute Handlerはサーバー側の処理。ブラウザからは直接Goを叩かない**
- **パスワードは必ずbcryptでハッシュ化。平文での保存・比較は絶対にしない**
- **JWTのシークレットキーは環境変数から読み込む。コードにハードコードしない**
- **CORSの設定をGoに忘れずに入れる（Next.jsフロントのURL: http://frontend:3000）**
- **go.modのGo versionは1.26.3**（2026年時点の最新版）
